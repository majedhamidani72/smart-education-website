# اسمارت اجوکیشن — وب‌سایت
پلتفرم آموزشی هوشمند برای دانش‌آموزان مدارس ایران.

## راه‌اندازی محلی
```bash
npm install
cp .env.local.example .env.local
npm run dev
```
سایت روی http://localhost:3000 بالا می‌آید. **باید بک‌اند لاراول هم هم‌زمان با `php artisan serve` روشن باشد** (روی پورت ۸۰۰۰).
